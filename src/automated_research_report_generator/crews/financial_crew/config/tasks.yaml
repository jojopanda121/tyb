extract_file_facts:
  extract_guidance: 优先提取利润表、资产负债表、现金流量表及附注中的分部信息、会计政策和非经常性损益；先做口径标准化再下结论。
  description: |-
    围绕 {company_name} 的 {pack_title} 执行 PDF 候选采集任务。

    输入：
    - PDF 文件路径：{pdf_file_path}
    - 页面索引文件路径：{page_index_file_path}
    - 当前 loop 原因：{loop_reason}
    - 当前专题焦点：{pack_focus}
    - 返工反馈：{qa_feedback}
    - 上游同行信息分析包：{peer_info_pack_text}

    执行要求：
    - 先读取页码索引，再有选择地读取相关页面，不要通读全文。
    - {extract_guidance}
    - 先用 `read_registry` 按 `owner_crew="{owner_crew}"` 读取当前 crew 的条目清单，优先关注 `unchecked / need_revision` 的 `high / medium` 条目。
    - 只处理当前 crew 负责的条目和与之直接相关的新发现，不要越权补写其他 pack。
    - 如果 `qa_feedback` 非空，优先回应其中属于文件提取、原文核对和页码补证的缺口；被标成 `need_revision` 的条目要结合 `revision_detail` 输出定向修订建议。
    - 你的职责是生成“候选 patch 报告”，而不是直接调用 `update_entry`、`add_entry`、`add_evidence` 或 `status_update`。
    - 对每个候选更新，优先匹配已有 `entry_id`，明确写出拟更新字段、原文摘录、页码或章节位置、时间口径和置信度；只有确认现有条目没有语义匹配槽位时，才把它列入 `new_entry_candidates`。
    - 如果 PDF 原文与外部资料或现有判断冲突，必须写清冲突来源、冲突点和当前判断，不能静默忽略。
    - 如果在 PDF 中无法提取到可靠信息，明确写“未提取到信息”，并把缺口归入 `unresolved_gaps`。
    - 任务结束时只输出供 `record_extract_registry` 落账的候选 patch 报告，不直接写 registry。
  expected_output: |-
    面向 manager 和后续 record task 的候选 patch 报告，必须严格包含以下三段：
    1. `existing_entry_updates`：逐条列出建议 amend 的 `entry_id`、拟更新字段、建议值、来源摘要、时间口径、置信度，以及是否涉及冲突。
    2. `new_entry_candidates`：仅列出确认没有现有语义匹配槽位的新增候选，写明建议 `entry_type`、标题、核心内容、来源摘要和为什么不能并入旧条目。
    3. `unresolved_gaps`：列出仍未解决的缺口、缺失原因、建议下一步走 `extract` 还是 `search`，以及是否影响后续综合输出。
  agent: extract_file_fact_agent
record_extract_registry:
  description: |-
    基于 `extract_file_facts` 的候选 patch 报告，执行 extract 结果的 registry 落账与修订任务。

    输入：
    - 当前责任 crew：{owner_crew}
    - 当前 loop 原因：{loop_reason}
    - 当前专题焦点：{pack_focus}
    - 返工反馈：{qa_feedback}
    - 上游上下文：`extract_file_facts` 的候选 patch 报告

    执行要求：
    - 先用 `read_registry` 按 `owner_crew="{owner_crew}"` 读取当前 crew 的现有条目和需要返工的 `entry_id`。
    - 只负责把上游已整理好的候选 patch 写回 registry，不再重新发散搜索，也不重新通读 PDF。
    - 对 `existing_entry_updates` 中的候选，优先使用 `update_entry` amend 既有 `entry_id`；必要时补齐 `unit`、`period`、`source`、`confidence`。
    - 对 `content_type="table"` 的条目，按 `columns` 完整更新表格行，不要只补半行字段。
    - 只有确认现有条目不存在语义匹配槽位时，才允许使用 `add_entry` 新建条目。
    - 对新增或实质更新后的结论，必须补 `add_evidence`；必要时用 `status_update` 写清 `revision_detail`。
    - 如果 `qa_feedback` 非空，优先关闭其中属于 PDF 提取与原文核对的缺口；无法关闭的条目要明确保留为 `need_revision`。
    - 任务结束前必须调用 `registry_review` 留痕，说明本轮 amend 了哪些条目、是否新建条目，以及哪些缺口仍未关闭。
  expected_output: |-
    面向 manager 的落账结果摘要：
    - 已 amend 的 `entry_id` 列表与关键更新字段
    - 已新建的 `entry_id` 列表与新增原因
    - 仍未关闭的缺口与原因
    - 建议下一步交回 `extract`、`search` 还是直接进入 `qa` 的事项
  agent: extract_file_fact_agent
search_facts:
  crew_name: financial_crew
  pack_name: finance_pack
  pack_title: 财务分析包
  pack_focus: 围绕收入结构与增长驱动、盈利质量与利润率趋势、现金流转换、营运资本效率和资产负债压力完成财务专题研究。
  output_title: 财务分析包
  search_guidance: 只有当 PDF 缺关键财务字段（如分部收入、现金流明细）时才补公开资料；补数据必须明确来源、口径和报告期。
  description: |-
    围绕 {company_name} 的 {pack_title} 执行外部搜索候选采集任务。

    输入：
    - 行业：{industry}
    - 当前责任 crew：{owner_crew}
    - 当前 loop 原因：{loop_reason}
    - 当前专题焦点：{pack_focus}
    - 返工反馈：{qa_feedback}
    - 上游同行信息分析包：{peer_info_pack_text}

    执行要求：
    - 先用 `read_registry` 按 `owner_crew="{owner_crew}"` 读取当前 crew 的条目清单，优先关注 `unchecked / need_revision` 的 `high / medium` 条目。
    - 必要时继续用 `view="entry_detail"` 读取具体 `entry_id`，确认现有条目的字段、口径和缺口，但本任务不得直接修改 registry。
    - 只处理当前 crew 负责的条目和与之直接相关的新发现，不要越权补写其他 pack。
    - {search_guidance}
    - 如果 `qa_feedback` 非空，优先回应其中属于外部搜索补证的缺口；被标成 `need_revision` 的条目要结合 `revision_detail` 输出定向修订建议。
    - 你的职责是生成“候选 patch 报告”，而不是直接调用 `update_entry`、`add_entry`、`add_evidence` 或 `status_update`。
    - 对每个候选更新，优先匹配已有 `entry_id`，明确写出拟更新字段、来源摘要、时间口径和置信度；只有确认现有条目没有语义匹配槽位时，才把它列入 `new_entry_candidates`。
    - 如果外部资料彼此冲突，必须写清冲突来源、冲突点和当前判断，不能静默忽略。
    - 如果没有搜到可靠资料，明确写“未搜索到信息”，并把缺口归入 `unresolved_gaps`。
    - 任务结束时只输出供 `record_search_registry` 落账的候选 patch 报告，不直接写 registry。
  expected_output: |-
    面向 manager 和后续 record task 的候选 patch 报告，必须严格包含以下三段：
    1. `existing_entry_updates`：逐条列出建议 amend 的 `entry_id`、拟更新字段、建议值、来源摘要、时间口径、置信度，以及是否涉及冲突。
    2. `new_entry_candidates`：仅列出确认没有现有语义匹配槽位的新增候选，写明建议 `entry_type`、标题、核心内容、来源摘要和为什么不能并入旧条目。
    3. `unresolved_gaps`：列出仍未解决的缺口、缺失原因、建议下一步走 `extract` 还是 `search`，以及是否影响后续综合输出。
  agent: search_fact_agent
record_search_registry:
  description: |-
    基于 `search_facts` 的候选 patch 报告，执行 search 结果的 registry 落账与修订任务。

    输入：
    - 当前责任 crew：{owner_crew}
    - 当前 loop 原因：{loop_reason}
    - 当前专题焦点：{pack_focus}
    - 返工反馈：{qa_feedback}
    - 上游上下文：`search_facts` 的候选 patch 报告

    执行要求：
    - 先用 `read_registry` 按 `owner_crew="{owner_crew}"` 读取当前 crew 的现有条目和需要返工的 `entry_id`。
    - 只负责把上游已整理好的候选 patch 写回 registry，不再重新发散搜索。
    - 对 `existing_entry_updates` 中的候选，优先使用 `update_entry` amend 既有 `entry_id`；必要时补齐 `unit`、`period`、`source`、`confidence`。
    - 对 `content_type="table"` 的条目，按 `columns` 完整更新表格行，不要只补半行字段。
    - 只有确认现有条目不存在语义匹配槽位时，才允许使用 `add_entry` 新建条目。
    - 对新增或实质更新后的结论，必须补 `add_evidence`；必要时用 `status_update` 写清 `revision_detail`。
    - 如果 `qa_feedback` 非空，优先关闭其中属于外部搜索补证的缺口；无法关闭的条目要明确保留为 `need_revision`。
    - 任务结束前必须调用 `registry_review` 留痕，说明本轮 amend 了哪些条目、是否新建条目，以及哪些缺口仍未关闭。
  expected_output: |-
    面向 manager 的落账结果摘要：
    - 已 amend 的 `entry_id` 列表与关键更新字段
    - 已新建的 `entry_id` 列表与新增原因
    - 仍未关闭的缺口与原因
    - 建议下一步交回 `extract`、`search` 还是直接进入 `qa` 的事项
  agent: search_fact_agent
check_registry:
  qa_guidance: 确认收入结构、利润质量（扣非后）、经营性现金流/净利润比率和资产负债压力是否已形成可支撑估值的定量结论。
  description: |-
    检查 {company_name} 的 {pack_title} 当前 registry 是否已经达到可综合输出的最低要求。

    执行要求：
    - 先用 `read_registry` 按 `owner_crew="{owner_crew}"` 读取当前 crew 的条目，并重点审查 `record_extract_registry` 与 `record_search_registry` 刚完成 amend 的结果。
    - {qa_guidance}
    - 找出仍是 `unchecked / need_revision` 的关键条目，尤其是 `high / medium` 优先级。
    - 如果发现遗漏，使用 `status_update` 补充更具体的 `revision_detail`，并在 `registry_review` 里写明建议回退给 `extract` 还是 `search`。
    - 你不负责新增正文内容；你只负责检查、标记和留痕。
    - 任务结束前必须调用 `registry_review` 留痕。
  expected_output: 面向 manager 的 QA 检查备忘录：整体就绪状态（ready/not ready）、未通过的 `entry_id` 及问题类型、修订建议、建议回退给 `extract` 还是 `search`。
  agent: qa_check_agent
synthesize_and_output:
  synthesize_guidance: 输出要明确收入驱动因子、盈利质量判断、现金转换效率、杠杆水平和对估值模型输入有直接影响的财务缺口。
  output_skeleton: |
    严格按下列格式形成一份结构化文本，严格使用下列标题作为标题，不得遗漏以下任何一个标题，不要增加任何标题和分节。必要时可以补充表格辅助阅读。
    每节要求写明：核心结论 / 关键证据或数据 / 冲突点 / 待确认事项。

    # {company_name} 财务分析
    ## 1. 核心财务数据总表
    - 1.1 近三年主要财务指标汇总（收入、净利润、经营性现金流等）
    - 1.2 数据口径说明（会计准则、报告期、是否经审计）
    ## 2. 收入与成本结构
    - 2.1 收入分部构成与各分部增速
    - 2.2 成本结构与毛利率变动趋势
    - 2.3 收入确认政策与关键假设
    ## 3. 盈利质量与利润率趋势
    - 3.1 扣非净利润与经常性/非经常性损益拆分
    - 3.2 毛利率、净利率、EBITDA 利润率趋势
    - 3.3 盈利质量判断（利润的现金含量）
    ## 4. 现金流与营运资本
    - 4.1 经营性现金流/净利润比率
    - 4.2 应收账款周转、存货周转和应付账款周转
    - 4.3 营运资本变动趋势与资金占用
    ## 5. 资产负债与资本结构
    - 5.1 资产负债率与有息负债规模
    - 5.2 短期偿债能力（流动比率、速动比率）
    - 5.3 资本结构与融资安排
    ## 6. 财务综合判断与估值影响
    - 6.1 对估值模型关键输入的影响（增长率、利润率、WACC 等）
    - 6.2 财务风险信号与关注事项
    - 6.3 仍缺失的关键财务数据
  description: |-
    基于 registry 中 {company_name} 的 {pack_title} 生成最终 Markdown 分析包。

    输入：
    - 行业：{industry}
    - 输出路径：{pack_output_path}
    - 当前专题焦点：{pack_focus}
    - 上游同行信息分析包：{peer_info_pack_text}

    执行要求：
    - 先用 `read_registry` 按 `owner_crew="{owner_crew}"` 读取当前 crew 的全部条目；必要时继续用 `view="entry_detail"` 下钻关键 `entry_id`。
    - 只综合当前 crew 负责的事实、数据、judgment 和相关证据，不得越权引用其他 pack 的未沉淀内容。
    - 本任务是严格只读综合：不得调用 `update_entry`、`add_entry`、`add_evidence` 或 `status_update` 修改 entry 内容、状态或证据。
    - 如果 `high / medium` 条目仍是 `unchecked / need_revision`，必须在正文和 `registry_review` 里显式写出缺口、影响和建议补证方向，不能偷偷补写 registry。
    - 明确区分：已被证据支持、存在冲突、仍未解决。
    - 如果存在冲突，要写明冲突点和严重程度，不要静默忽略。
    - {synthesize_guidance}
    - 任务结束前必须调用 `registry_review` 留痕，说明本轮综合输出是否仍有残余缺口。
  expected_output: |
    {output_skeleton}
  agent: synthesizing_agent
  output_file: '{pack_output_path}'
